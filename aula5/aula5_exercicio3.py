from math import radians, sin, con, tan
angulo=float(input('Digite aqui um valor para o ângulo: '))
seno=sin(radians(angulo))
cosseno=con(radians(angulo))
tangente=tan(radians(angulo))
print('Para o ângulo {} o seno é, o cosseno {} e a tangente é {}.'.format(angulo, seno, con, tan))